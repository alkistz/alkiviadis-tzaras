// The whole site is static HTML: every route is prerendered at build time and
// no JavaScript is shipped to the browser. Motion is pure CSS.
export const prerender = true;
export const csr = false;
